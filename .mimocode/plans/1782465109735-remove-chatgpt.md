# Plan: Remove ChatGPT Feature

## Overview
Remove the ChatGPT integration entirely (chat threads + image generation) and add a DB migration to clean up existing data.

## Files to Delete
- `neonbot/classes/chatgpt/chatgpt.py`
- `neonbot/classes/chatgpt/chat_thread.py`
- `neonbot/models/chatgpt.py`

## Files to Modify

### 1. `neonbot/models/guild.py`
- Remove `from neonbot.models.chatgpt import ChatGPTModel` import
- Remove `chatgpt: Optional[ChatGPTModel] = None` field
- Remove `chatgpt=ChatGPTModel(chats=[])` from `create_default_collection()`

### 2. `neonbot/cogs/event.py`
- Remove `from neonbot.classes.chatgpt.chatgpt import ChatGPT` import (line 15)
- Remove the `if await ChatGPT().create_thread(ctx): return` block (lines 73-74)

### 3. `neonbot/cogs/search.py`
- Remove `from neonbot.classes.chatgpt.chatgpt import ChatGPT` import (line 16)
- Remove `chatgpt` command group definition (line 30)
- Remove `chatgpt_image` command method (lines 427-439)

### 4. `neonbot/cogs/administration.py`
- Remove `set-chatgpt` command (lines 211-232)

### 5. `neonbot/classes/database.py`
- Remove `from neonbot.classes.chatgpt.chatgpt import ChatGPT` import
- Remove `await ChatGPT.cleanup_threads(guild)` call (line 110)

### 6. `neonbot/lang/administration.en.json`
- Remove 4 chatgpt-related i18n keys (lines 9-12)

### 7. `pyproject.toml`
- Remove `openai (>=2.21.0,<3.0.0)` dependency (line 22)
- Remove `tiktoken (>=0.12.0,<0.13.0)` dependency (line 23)

### 8. `.env.example`
- Remove `OPENAI_API_KEY=`, `OPENAI_MODEL=`, `OPENAI_IMAGE_MODEL=`, `OPENAI_MAX_TOKEN=` (lines 36-39)

### 9. `neonbot/utils/constants.py`
- Remove `'openai'` entry from ICONS dict (line 39)

## New File

### `neonbot/migrations/002_drop_chatgpt.py`
```python
from pymongo.asynchronous.database import AsyncDatabase


async def up(db: AsyncDatabase) -> None:
    await db['guilds'].update_many({}, {'$unset': {'chatgpt': ''}})
```
This uses `$unset` to remove the `chatgpt` field from all guild documents, following the existing migration pattern.

## Verification
1. Run `ruff check neonbot/` to verify no broken imports or references
2. Run `ruff format neonbot/` for consistent formatting
3. Grep for any remaining `chatgpt` references: `grep -ri chatgpt neonbot/`
4. Verify migration file follows the `NNN_description.py` pattern and exposes `async def up(db)`
